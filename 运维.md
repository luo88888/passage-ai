# 运维指南

## 1 启动命令

### 后端

```bash
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8567
```

### 前端

```bash
npm run dev
```

## API 接口文档

访问 <http://localhost:8567/docs> 查看 FastAPI 自动生成的 Swagger 文档。
